class Selector {
    constructor(titleSelector, urlSelector, imgSelector) {
        this.titleSelector = titleSelector;
        this.urlSelector = urlSelector;
        this.imgSelector = imgSelector;
    }
}

module.exports = Selector;